# 弦音序（Notekey）· 产品需求文档（PRD）

> 项目：弦音序（Notekey）—— 网页版钢琴谱播放器  
> 文档版本：v1.0（Day 4）  
> 日期：2026-09-22  
> 上游文档：`research.md`（Day 3 需求研究）  
> 下游文档：技术设计文档（Day 5，本文档不含技术选型）

---

## 一、产品概述

### 1.1 一句话描述

**弦音序**是一个**在浏览器中运行**的钢琴谱播放器：用户搜索曲目 → 进入播放页 → **五线谱与虚拟钢琴键盘同步联动播放**。

### 1.2 要解决的核心问题

来自 `research.md` 的竞品分析结论，市场存在**结构性缺口**：

| 现有产品          | 给了什么      | 缺了什么                    |
| ------------- | --------- | ----------------------- |
| MuseScore     | 专业五线谱     | 没有虚拟键盘联动                |
| Synthesia     | 键盘联动（瀑布流） | **不给五线谱**               |
| OnlinePianist | 网页版虚拟键盘   | 谱面与键盘割裂                 |
| 虫虫钢琴          | 谱面 + 键盘同屏 | **布局比例失衡**（谱面占太大、键盘占太小） |

**弦音序要补的缺口**：把"**五线谱**"和"**虚拟键盘联动**"这两个长期分家的东西，**缝在一个网页里**，并**解决布局比例问题**。

### 1.3 本期定位（做什么 / 不做什么）

**做**：

- ✅ 网页端，打开浏览器即用，无需安装
- ✅ 五线谱 + 虚拟钢琴键盘**同步联动播放**
- ✅ 布局比例**用户可拖拽调节**
- ✅ 基础播放控制（播放/暂停、进度拖拽、变速、分轨、节拍器）
- ✅ 免费，无付费墙

**不做**（详见第四节）：

- ❌ 真实大规模曲库数据源
- ❌ 账号系统、收藏、社区
- ❌ 跟弹 / 练习判定模式
- ❌ 曲谱编辑

---

## 二、目标用户

### 2.1 核心用户画像

**主用户：会看五线谱的钢琴爱好者**

| 维度       | 描述                                       |
| -------- | ---------------------------------------- |
| **是谁**   | 学过钢琴、认识五线谱，但并非专业演奏者；或正在学琴、需要看谱听曲的学生      |
| **设备**   | 主要在**电脑**上使用（屏幕大，能同时看谱和键盘）               |
| **使用场景** | ① 想听某首曲子怎么弹 ② 对着谱子确认"这个音在哪" ③ 想看键盘演示帮助识谱 |
| **现有困扰** | 手机 App 要付费；屏幕小看不清；谱面和键盘挤在一起看不舒服          |
| **技术水平** | 不需要懂技术，会打字搜索即可                           |

### 2.2 明确的非目标用户

| 不是给谁        | 原因                             |
| ----------- | ------------------------------ |
| 完全不识谱的纯小白   | 本产品以五线谱为核心，Synthesia 的瀑布流更适合他们 |
| 专业演奏者 / 编曲者 | 他们需要的是 MuseScore 这类制谱工具        |
| 想创作 / 打谱的人  | 本产品不做编辑功能                      |
| 手机端重度用户     | 本期以电脑端体验为主（移动端可基本使用，但不是优化重点）   |

---

## 三、MVP 功能（本期必做）

> 每个功能包含：**用户动作**（用户做什么）+ **可观察结果**（用户能看到什么）。

### 3.1 曲库页

#### 功能 3.1.1 曲目搜索

| 项         | 内容                                                     |
| --------- | ------------------------------------------------------ |
| **用户动作**  | 在曲库页顶部搜索框输入关键词（如"爱""贝多芬"）                              |
| **可观察结果** | 输入后，曲目列表**实时筛选**，只显示**曲名或作曲者**包含该关键词的曲目；清空搜索框后恢复显示全部曲目 |
| **搜索范围**  | 曲名 + 作曲者（两字段模糊匹配，不区分大小写）                               |
| **无结果时**  | 列表区域显示"未找到相关曲目"，并提供"清空搜索"按钮                            |

#### 功能 3.1.2 曲目列表

| 项         | 内容                                     |
| --------- | -------------------------------------- |
| **用户动作**  | 浏览曲目列表                                 |
| **可观察结果** | 每首曲目以卡片形式展示，卡片上**同时可见**：曲名、作曲者、级数标签、时长 |
| **排序**    | 默认按**曲目级数由低到高**排列                            |
| **点击卡片**  | 进入该曲目的播放页                              |

#### 功能 3.1.3 曲目数量（分阶段）

| 阶段              | 曲目数          | 目的                      |
| --------------- | ------------ | ----------------------- |
| **阶段一（先做）**     | **3 首**      | 打通"谱面 + 键盘联动"完整链路，验证可行性 |
| **阶段二（链路通了再补）** | 补齐至 **10 首** | 让曲库页的列表、搜索、筛选有足够数据可展示   |

**曲目要求**：均为**公版曲目**（无版权风险），例如致爱丽丝、小星星、卡农、欢乐颂、月光奏鸣曲等。

> ✅ **已同步**：`research.md` 中的曲目数已同步更新为「10 首（分阶段）」，两份文档一致。

---


### 3.2 播放页

#### 功能 3.2.1 五线谱显示

| 项         | 内容                                                       |
| --------- | -------------------------------------------------------- |
| **用户动作**  | 从曲库页点击某首曲目，进入播放页                                         |
| **可观察结果** | 页面上半部分显示该曲目的**完整五线谱**（双手，含谱号、调号、拍号、音符、小节线）；谱面可横向滚动查看后续内容 |
| **默认状态**  | 进入页面时，谱面显示在**开头位置**，播放头停在第一个音符处                          |

#### 功能 3.2.2 虚拟钢琴键盘显示

| 项         | 内容                                                  |
| --------- | --------------------------------------------------- |
| **用户动作**  | 查看页面下半部分的键盘区域                                       |
| **可观察结果** | 显示**88 键**钢琴键盘（白键黑键正确排布）；当键盘宽度超出屏幕时，可**横向滚动**查看全部琴键 |
| **滚动到边界** | 到达最左/最右时停止，不出现空白区域                                  |

#### 功能 3.2.3 布局比例拖拽调节 ← 核心差异化

| 项         | 内容                                              |
| --------- | ----------------------------------------------- |
| **用户动作**  | 拖动谱面区与键盘区之间的**分隔条**                             |
| **可观察结果** | 上下两区高度**实时变化**；拖到极端位置时，任一方不小于可用高度的 20%（防止一方被拖没） |
| **持久化**   | 调整后的比例在**本次会话内保持**（切换曲目后仍生效）                    |

#### 功能 3.2.4 视觉联动播放 ← 核心功能

| 项         | 内容                                                                                                                  |
| --------- | ------------------------------------------------------------------------------------------------------------------- |
| **用户动作**  | 点击播放按钮                                                                                                              |
| **可观察结果** | **三个动作同时发生且保持同步**：  
① **谱面**：当前正在播放的音符高亮显示，播放头随播放前进  
② **键盘**：与该音符对应的琴键变色点亮，音符结束后熄灭  
③ **声音**：听到对应的钢琴音，音高与谱面音符一致 |
| **谱面跟随**  | 播放头前进到超出可视区域时，谱面**自动滚动**，使播放头保持在可视范围内                                                                               |
| **和弦处理**  | 同时发声的多个音符，其对应琴键**同时点亮**                                                                                             |

#### 功能 3.2.5 左右手分轨（视觉 + 听觉）

| 项           | 内容                                                                        |
| ----------- | ------------------------------------------------------------------------- |
| **用户动作**    | 切换分轨模式：双手 / 仅左手 / 仅右手                                                     |
| **可观察结果**   | ① **听觉**：仅播放所选声部，另一声部静音  
② **视觉**：谱面与键盘上，**左手音符与右手音符使用不同颜色区分**（全模式下一目了然） |
| **默认状态** | 进入播放页时默认"双手"模式，谱面与键盘上左右手颜色可区分                                             |

---

### 3.3 播放控制

| 编号    | 功能          | 用户动作               | 可观察结果                                                           |
| ----- | ----------- | ------------------ | --------------------------------------------------------------- |
| 3.3.1 | **播放 / 暂停** | 点击播放或暂停按钮          | 播放时按钮变为"暂停"图标，音符与琴键开始联动；暂停时画面停在当前位置，再次点击从**暂停处继续**              |
| 3.3.2 | **进度条拖拽**   | 拖动进度条，或点击进度条某位置    | 播放位置**跳转**到对应位置；谱面同步滚动到对应位置；键盘高亮状态同步更新；**拖动时若正在播放，松手后从新位置继续播放** |
| 3.3.3 | **速度调节**    | 调节速度控制（**范围 50%–150%**） | 播放速度**实时改变**，声音与联动节奏同步变快或变慢；调速后当前播放位置**不跳回开头**                  |
| 3.3.4 | **节拍器开关**   | 点击节拍器按钮            | 开启后听到规律节拍声，节拍速度**跟随当前曲目速度**；关闭后停止发声                             |
| 3.3.5 | **时间显示**    | 观察播放页              | 显示"当前时间 / 总时长"（如 `01:23 / 03:45`），随播放实时更新                       |

---

## 四、后续功能（本期不做）

> 明确写出"不做什么"，避免范围失控。这些不是被抛弃，而是**排到后面**。

| 功能                | 为什么本期不做                                     |
| ----------------- | ------------------------------------------- |
| **收藏曲目**          | 需要账号体系或本地存储设计，不影响核心体验验证；**列为后续第一批**         |
| **真实大规模曲库数据源**    | 版权、文件体积、逐首验证成本高，28 天无法完成（`research.md` 已论证） |
| **用户上传曲谱**        | 需要文件存储、格式校验、审核流程                            |
| **账号 / 登录 / 注册**  | 与核心价值无关，且需要后端服务                             |
| **跟弹 / 练习判定模式**   | 需要外接 MIDI 设备 + 实时判定逻辑，工作量翻倍                 |
| **社区 / 评论 / 分享**  | 做社区需要持续的人力运营，不是 28 天项目该碰的                   |
| **曲谱编辑 / 制谱**     | 这是 MuseScore 的领域，与本产品定位冲突                   |
| **移动端 App**       | 本期只做网页                                      |
| **真实演奏录音（高保真音源）** | 本期只用合成音                                     |
| **离线使用 / 安装为应用**  | 属优化项，不在核心链路                                 |
| **付费 / 会员体系**     | 与"免费解决付费痛点"的初衷相悖                            |
| **乐理教学 / 指法提示**   | 属教学功能，超出"看谱听曲"范围                            |

---

## 五、页面与交互要求

### 5.1 页面清单

本期共 **2 个页面**：

| 页面      | 路径概念   | 作用         |
| ------- | ------ | ---------- |
| **曲库页** | 首页     | 搜索 + 浏览曲目  |
| **播放页** | 由曲库页进入 | 看谱、听曲、操作播放 |

### 5.2 曲库页布局

```
┌─────────────────────────────────────────┐
│  弦音序                    [搜索框]       │  ← 顶部：品牌名 + 搜索框
├─────────────────────────────────────────┤
│  ┌────────┐  ┌────────┐  ┌────────┐     │
│  │ 曲名    │  │ 曲名    │  │ 曲名    │     │  ← 曲目卡片网格
│  │ 作曲者  │  │ 作曲者  │  │ 作曲者  │     │
│  │ 级数·时长│ │ 级数·时长│ │ 级数·时长│     │
│  └────────┘  └────────┘  └────────┘     │
│  ┌────────┐  ┌────────┐  ┌────────┐     │
│  │  ...   │  │  ...   │  │  ...   │     │
└─────────────────────────────────────────┘
```

**交互要求**：

- 搜索框输入即时筛选，无需点"搜索"按钮
- 卡片有明确的悬停反馈（提示可点击）
- 点击卡片进入播放页

### 5.3 播放页布局

```
┌─────────────────────────────────────────┐
│  ← 返回    曲名 · 作曲者                  │  ← 顶部：返回 + 曲目信息
├─────────────────────────────────────────┤
│                                         │
│         五 线 谱 区 域                   │  ← 上区：谱面（可横向滚动）
│     （当前音符高亮 + 播放头）             │
│                                         │
├═══════════════ ⇕ ═══════════════════════┤  ← 分隔条（可拖拽）
│                                         │
│      虚 拟 钢 琴 键 盘（88 键）           │  ← 下区：键盘（可横向滚动）
│                                         │
├─────────────────────────────────────────┤
│  ▶/⏸  ──●──────  01:23/03:45  1.0x  分轨  节拍器 │  ← 底部：播放控制条
└─────────────────────────────────────────┘
```

**交互要求**：

- 分隔条可上下拖拽，实时改变两区高度
- 键盘与谱面的滚动**相互独立**（各自滚动，不联动滚动）
- 播放时谱面自动跟随播放头滚动
- 播放控制条固定在底部，不随内容滚动

### 5.4 关键交互规则

| 规则        | 说明                                     |
| --------- | -------------------------------------- |
| **联动精度**  | 谱面高亮、琴键点亮、声音三者**必须在同一时刻发生**，不允许肉眼可见的错位 |
| **响应速度**  | 点击播放后**立即**开始，无可见延迟                    |
| **拖拽进度条** | 拖动过程中可实时预览位置（谱面与键盘跟随更新）                |
| **返回曲库页** | 从播放页返回后，曲库页的搜索条件**保持**（不重置）            |

---

## 六、数据字段

### 6.1 曲目数据（每首曲目一条）

| 字段            | 类型    | 必填 | 说明                       |
| ------------- | ----- | -- | ------------------------ |
| `id`          | 文本/数字 | ✅  | 曲目唯一标识                   |
| `title`       | 文本    | ✅  | 曲名，如"致爱丽丝"               |
| `composer`    | 文本    | ✅  | 作曲者，如"贝多芬"               |
| `difficulty`  | 数值    | ✅  | **曲目级数**（参照钢琴考级标准，取值 1–10），数值越小越简单 |
| `duration`    | 时长    | ✅  | 曲目总时长（用于进度条与时间显示）        |
| `sheetFile`   | 文件引用  | ✅  | **五线谱数据文件**（用于渲染谱面）      |
| `midiFile`    | 文件引用  | ✅  | **MIDI 数据文件**（用于驱动播放与联动） |
| `description` | 文本    | ❌  | 曲目简介（可选）                 |
| `coverImage`  | 文件引用  | ❌  | 卡片缩略图（可选，无图时用默认样式）       |

> ⚠️ **注意**：每首曲目需要**两份数据文件**（谱面数据 + MIDI 数据），两者必须描述**同一首曲子的同一版本**，否则联动会错位。

### 6.2 播放状态数据（播放页运行时）

| 字段                | 类型    | 说明                        |
| ----------------- | ----- | ------------------------- |
| `currentTrackId`  | 文本/数字 | 当前播放的曲目 id                |
| `isPlaying`       | 布尔    | 是否正在播放                    |
| `currentPosition` | 时长    | 当前播放位置（用于进度条与时间显示）        |
| `playbackSpeed`   | 数值    | 播放速度倍率（默认 1.0）            |
| `trackMode`       | 枚举    | 分轨模式：`双手` / `仅左手` / `仅右手` |
| `metronomeOn`     | 布尔    | 节拍器是否开启                   |
| `layoutRatio`     | 数值    | 谱面区高度占比（拖拽结果，默认约 55%）     |

### 6.3 界面状态数据

| 字段                  | 类型 | 说明                       |
| ------------------- | -- | ------------------------ |
| `searchKeyword`     | 文本 | 曲库页当前搜索关键词               |
| `searchResultCount` | 数字 | 当前筛选结果数量（用于"无结果"判断）      |
| `loadingState`      | 枚举 | 加载状态：`加载中` / `成功` / `失败` |
| `errorMessage`      | 文本 | 出错时向用户展示的提示文案            |

---

## 七、异常状态

> 每个异常都要定义"**用户看到什么**"，不能出现白屏或无声失败。

| 编号  | 异常场景         | 触发条件                 | 用户看到什么                                    |
| --- | ------------ | -------------------- | ----------------------------------------- |
| E1  | **搜索无结果**    | 关键词匹配不到任何曲目          | 列表区显示"未找到相关曲目，试试其他关键词"，并提供"清空搜索"按钮        |
| E2  | **曲目数据加载失败** | 谱面文件或 MIDI 文件读取失败    | 播放页显示"曲目加载失败，请返回重试"，提供"返回曲库"按钮；**不显示空白页** |
| E3  | **曲目文件缺失**   | 数据记录存在但文件不存在         | 与 E2 相同提示；同时在控制台记录具体缺失的文件名（供开发者排查）        |
| E4  | **谱面解析失败**   | 谱面数据格式异常             | 谱面区显示"谱面无法显示"，但**键盘与播放仍可用**（降级而非全挂）       |
| E5  | **音频无法播放**   | 浏览器不支持音频 / 自动播放被拦截   | 页面顶部提示"点击任意位置以启用声音"；用户点击后恢复正常播放           |
| E6  | **两份数据不匹配**  | 谱面与 MIDI 的音符数或时长差异过大 | 播放仍可进行，但在页面顶部显示警告："谱面与音频可能存在偏差"（不阻断使用）    |
| E7  | **拖动进度到末尾**  | 进度条拖到 100%           | 播放自动停止，按钮回到"播放"状态，播放头停在最后一个音符处            |
| E8  | **键盘滚动到边界**  | 横向滚动超出琴键范围           | 停止滚动，不出现空白；可滚动时显示滚动提示（如边缘阴影）              |
| E9  | **曲库为空**     | 曲目数据列表为空             | 曲库页显示"曲库暂无内容"，不显示空白网格                     |
| E10 | **拖拽分隔条到极端** | 拖到最上或最下              | 任一方不低于可用高度的 20%，**不允许把一方完全拖没**            |

---

## 八、验收标准

> 以下每条均可**逐条打勾**。只描述"能看到什么"，不描述"怎么实现"。
>
> **编号规则**（避免与第七节异常场景编号混淆）：
> - 功能验收：`A` 曲库页 / `B` 播放页显示 / `C` 布局拖拽 / `D` 视觉联动 / `P` 播放控制 / `F` 分轨 / `G` 异常状态 / `H` 整体
> - 第七节**异常场景**编号为 `E1–E10`，本节不再使用 `E` 前缀。

### 8.1 曲库页

- [ ] **A1** 打开首页，能看到顶部搜索框和曲目列表
- [ ] **A2** 曲目卡片上同时可见：曲名、作曲者、级数、时长（四项齐全）
- [ ] **A3** 搜索框输入曲名的一部分（如"爱"），列表只显示曲名包含该词的曲目
- [ ] **A4** 搜索框输入作曲者的一部分（如"贝多芬"），列表只显示作曲者包含该词的曲目
- [ ] **A5** 搜索大小写不敏感（如输入 `CHOPIN` 能匹配到 `Chopin`）
- [ ] **A6** 输入无匹配的关键词时，显示"未找到相关曲目"提示，而非空白
- [ ] **A7** 点击"清空搜索"后，列表恢复显示全部曲目
- [ ] **A8** 点击任意曲目卡片，能进入该曲目的播放页
- [ ] **A9** 鼠标悬停在曲目卡片上时，卡片出现明显的视觉反馈（如变色、阴影、鼠标指针变为手型），提示可点击
- [ ] **A10** 曲目列表默认**按级数从低到高**排列（第一首的级数不高于第二首，依此类推）

### 8.2 播放页 · 显示

- [ ] **B1** 播放页同时显示五线谱和虚拟钢琴键盘，两者上下排列
- [ ] **B2** 五线谱显示双手谱表，含谱号、调号、拍号、音符、小节线
- [ ] **B3** 五线谱可横向滚动，能查看完整曲目
- [ ] **B4** 虚拟键盘显示 **88 键**，白键黑键排布正确
- [ ] **B5** 键盘宽度超出屏幕时可横向滚动，能到达最低音与最高音；滚到两端时停止，**不出现空白区域**
- [ ] **B6** 页面顶部显示曲名与作曲者
- [ ] **B7** 页面有"返回曲库"入口
- [ ] **B8** 播放控制条固定在页面底部，滚动谱面或键盘区域时，控制条位置**不变**
- [ ] **B9** 滚动谱面区域时，键盘区域**不跟随滚动**；滚动键盘区域时，谱面区域**不跟随滚动**（两者滚动相互独立）
- [ ] **B10** 进入播放页时，谱面显示在**开头位置**，播放头停在**第一个音符**处

### 8.3 播放页 · 布局拖拽（核心差异化）

- [ ] **C1** 谱面区与键盘区之间有可拖拽的分隔条
- [ ] **C2** 拖动分隔条时，两区高度实时变化
- [ ] **C3** 分隔条拖到极端位置时，任一区高度不低于可用高度的 20%
- [ ] **C4** 调整比例后切换曲目再返回，比例**保持不变**

### 8.4 播放页 · 视觉联动（核心功能）

- [ ] **D1** 点击播放后，**能听到声音**，且音高与谱面音符一致
- [ ] **D2** 播放时，谱面上**当前音符高亮**，高亮随播放前进
- [ ] **D3** 播放时，键盘上**对应的琴键点亮**，音符结束后熄灭
- [ ] **D4** 谱面高亮、琴键点亮、声音三者**同步**：在任意位置**暂停**，谱面高亮音符与键盘点亮琴键指向**同一个音**，无错位
- [ ] **D5** 遇到和弦（多音同时）时，多个琴键**同时点亮**
- [ ] **D6** 播放头超出可视范围时，谱面**自动滚动**跟随
- [ ] **D7** 暂停后，高亮与点亮状态**停在当前位置**不消失

### 8.5 播放页 · 播放控制

- [ ] **P1** 点击播放按钮开始播放，按钮变为暂停图标
- [ ] **P2** 点击暂停按钮停止播放，按钮变回播放图标
- [ ] **P3** 暂停后再次点击播放，**从暂停处继续**（不回到开头）
- [ ] **P4** 拖动进度条，播放位置跳转到对应位置
- [ ] **P5** 拖动进度条时，谱面滚动位置与键盘高亮同步更新
- [ ] **P6** 正在播放时拖动进度条，松手后从新位置继续播放
- [ ] **P7** 调节速度后，播放速度实时改变，声音与联动节奏同步变化
- [ ] **P8** 调速后，当前播放位置**不跳回开头**
- [ ] **P9** 速度可调范围为 **50%–150%**，两端均可达
- [ ] **P10** 开启节拍器后能听到规律节拍声
- [ ] **P11** 节拍器的节拍速度**跟随当前曲目速度**（调速后节拍同步变化）
- [ ] **P12** 关闭节拍器后节拍声停止
- [ ] **P13** 显示"当前时间 / 总时长"，随播放实时更新
- [ ] **P14** 点击播放按钮后**立即**开始播放，无需二次点击或额外等待

### 8.6 播放页 · 分轨

- [ ] **F1** 可切换三种模式：双手 / 仅左手 / 仅右手
- [ ] **F2** 选择"仅左手"时，只听到左手声部，右手静音
- [ ] **F3** 选择"仅右手"时，只听到右手声部，左手静音
- [ ] **F4** 谱面上左手音符与右手音符**颜色不同**，可区分
- [ ] **F5** 键盘上左手音与右手音点亮时**颜色不同**，可区分
- [ ] **F6** 默认进入播放页时为"双手"模式

### 8.7 异常状态

- [ ] **G1** 曲目数据加载失败时，显示"曲目加载失败"提示，不出现白屏
- [ ] **G2** 谱面解析失败时，键盘与播放仍可使用（降级而非全挂）
- [ ] **G3** 音频被浏览器拦截时，显示"点击以启用声音"提示
- [ ] **G4** 进度条拖到 100% 时，播放自动停止，按钮回到播放状态
- [ ] **G5** 曲库为空时，显示"曲库暂无内容"，不显示空白网格
- [ ] **G6** 谱面与音频时长差异过大时，页面顶部显示"谱面与音频可能存在偏差"警告，且**不阻断播放**

### 8.8 整体

- [ ] **H1** 全部页面在电脑端浏览器（Chrome / Edge）中正常显示
- [ ] **H2** **阶段一**验收：曲库包含 **3 首**曲目，且每首均可正常播放
- [ ] **H3** **全部完成**验收：曲库包含 **10 首**曲目
- [ ] **H4** 每首曲目均可正常播放，且联动正常
- [ ] **H5** 从曲库页 → 播放页 → 返回曲库页，搜索条件保持
- [ ] **H6** 全程无付费提示、无登录要求

---

## 九、附：待确认事项

| 编号 | 待确认内容                                       | 影响                      |
| -- | ------------------------------------------- | ----------------------- |
| ~~Q1~~ | ~~`research.md` 与本文档曲目数不一致~~ | ✅ **已解决**：`research.md` 已同步为「10 首（分阶段）」 |
| Q2 | 具体 10 首曲目的曲名与作曲者清单（**今日不定**）| 影响数据准备（Day 6 前确定即可）|
| ~~Q3~~ | ~~难度分级的判定依据~~ | ✅ **已解决**：按**曲目级数**（参照钢琴考级，1–10 级）|
| Q4 | 谱面与 MIDI 的"同一版本"如何保证（两份文件来源）                | 影响联动精度（技术设计阶段解决）        |

---

*文档结束。下一步：Day 5 技术设计（技术选型、架构、数据流）。*
