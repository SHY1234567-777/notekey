# 弦音序（Notekey）· 技术设计文档（TECH_DESIGN）

> 项目：弦音序（Notekey）—— 网页版钢琴谱播放器
> 文档版本：v1.0（Day 5）
> 日期：2026-09-22
> 上游文档：`PRD.md`（Day 4 产品需求文档）、`research.md`（Day 3 需求研究）
> 本文档范围：技术选型、项目结构、数据对象、接口约定、数据流、错误处理、环境变量、部署与迁移
> **本文档不含代码实现**（Day 7 开始）

---

## 一、技术选型与理由

### 1.1 约束条件

| 约束 | 说明 |
|---|---|
| **时间** | 28 天 |
| **基础** | 零基础，需要 AI 代写代码并看得懂 |
| **成本** | 不希望产生服务器费用 |
| **形态** | 网页（不是桌面软件，不是原生 App）|
| **核心功能** | 五线谱 + 虚拟键盘同步联动播放（**实时性要求高**）|

### 1.2 三套候选方案比较

#### 方案 A：纯静态（原生 HTML/CSS/JS）

| 维度 | 说明 |
|---|---|
| 前端 | 原生 HTML / CSS / JavaScript |
| 后端 | 无 |
| 数据库 | 无（数据以静态文件形式打包）|
| 部署 | GitHub Pages |
| **优势** | 零成本、最容易上手、无需服务器运维、部署只需推送代码 |
| **劣势** | 页面逻辑变多后代码难以维护；无后端能力 |
| **28 天可行性** | 高 |

#### 方案 B：前端框架 + 静态托管（Vue）

| 维度 | 说明 |
|---|---|
| 前端 | Vue 3 |
| 后端 | 无 |
| 数据库 | 无 |
| 部署 | GitHub Pages / Vercel / Netlify |
| **优势** | 数据变化自动更新界面，适合"播放时界面实时跟着变"的场景；代码组织清晰 |
| **劣势** | 需要学习 Vue 语法；需要构建步骤（多一层工具链）|
| **28 天可行性** | 高 |

#### 方案 C：全栈（前端框架 + 后端 + 数据库）

| 维度 | 说明 |
|---|---|
| 前端 | Vue 3 |
| 后端 | Node.js |
| 数据库 | 需要（如 SQLite / PostgreSQL）|
| 部署 | 云服务器（**需付费**）|
| **优势** | 支持账号系统、用户上传共享、数据动态管理 |
| **劣势** | 需要服务器费用与运维；涉及后端安全、数据库设计；**工作量为方案 B 的 2–3 倍** |
| **28 天可行性** | **低（不可行）** |

### 1.3 推荐路线（本项目采用）

> **采用：方案 B 的架构 —— Vue 3 前端 + 无后端 + 静态数据 + GitHub Pages 部署**
>
> 实质是「方案 A 的无后端 + 方案 B 的前端框架」结合。

| 层级 | 选型 |
|---|---|
| **前端框架** | **Vue 3**（使用 `<script setup>` 写法）|
| **后端** | **无** |
| **数据库** | **无** |
| **数据存储** | **JSON 文件 + 资源文件夹**（打包进项目）|
| **部署平台** | **GitHub Pages** |
| **构建工具** | Vite（Vue 官方推荐，用于开发与打包）|

### 1.4 各选择项的理由与取舍

| 选择 | 理由 | 代价（诚实说明） |
|---|---|---|
| **无后端** | ① 数据固定（10 首曲目），不需要动态查询 ② 不需要用户账号 ③ **0 成本**，不用买服务器 ④ 不用处理服务器安全和运维 ⑤ 部署只需推送代码 | ❌ 做不了账号系统<br>❌ 做不了"用户上传到网站共享"<br>❌ 做不了收藏云同步<br>❌ 曲目更新需要改代码后重新推送 |
| **Vue 3** | ① 核心功能是"播放位置变化 → 谱面高亮 + 键盘点亮同时更新"，**这正是 Vue 响应式数据的强项**（数据一变，界面自动更新，不用手写滚动监听）② 比 React 概念少，中文资料多 ③ 组件化让"谱面区""键盘区""控制条"各自独立，好维护 | ⚠️ 需要学 Vue 语法（模板、响应式、组件）<br>⚠️ 引入了构建工具链（多一层概念）|
| **JSON 存曲目数据** | ① 数据结构清晰、可读性好 ② 编辑方便（加一首歌只需改 JSON）③ 工具支持完善 | ⚠️ 手写 JSON 容易漏逗号等语法错误 |
| **GitHub Pages** | ① 免费 ② 与现有仓库天然集成（推代码即上线）③ 无需配置服务器 | ⚠️ 只能托管静态文件（与"无后端"一致）<br>⚠️ 国内访问速度可能较慢 |

### 1.5 明确不采用的技术（以及为什么）

| 不采用 | 原因 |
|---|---|
| React | 概念更多（Hooks、状态管理库），零基础学习曲线更陡，28 天内收益不明显 |
| 后端框架（Express / Koa 等）| 本项目无后端需求 |
| 数据库（MySQL / MongoDB 等）| 数据固定，无动态读写需求 |
| 云服务器 | 需要费用与运维，与"0 成本"目标冲突 |
| 桌面应用框架（Electron / Tauri）| 项目形态是网页，不需要打包为桌面应用 |

---

## 二、项目结构

### 2.1 目录结构

```
弦音序项目根目录/
├── index.html                  # 网页入口文件
├── package.json                # 项目依赖与脚本配置
├── vite.config.js              # 构建工具配置
├── .gitignore                  # 忽略规则（已存在）
├── AGENTS.md                   # 项目规则（Day 1）
├── research.md                 # 需求研究（Day 3）
├── PRD.md                      # 产品需求文档（Day 4）
├── TECH_DESIGN.md              # 本文档（Day 5）
│
├── public/                     # 静态资源（不经过构建处理，直接复制）
│   ├── sheets/                 # 乐谱文件（MusicXML 格式）
│   │   ├── track-01.musicxml
│   │   ├── track-02.musicxml
│   │   └── ...
│   ├── midi/                   # MIDI 文件（驱动播放）
│   │   ├── track-01.mid
│   │   ├── track-02.mid
│   │   └── ...
│   └── covers/                 # 曲目封面图（可选）
│       └── ...
│
└── src/                        # 源代码目录
    ├── main.js                 # 程序入口（挂载 Vue 应用）
    ├── App.vue                 # 根组件
    │
    ├── assets/                 # 需要构建处理的资源（样式、字体等）
    │   └── styles/
    │       └── main.css        # 全局样式
    │
    ├── data/                   # 数据层
    │   └── tracks.json         # 曲目数据（10 首曲目的元信息）
    │
    ├── views/                  # 页面级组件
    │   ├── LibraryView.vue     # 曲库页
    │   └── PlayerView.vue      # 播放页
    │
    ├── components/             # 可复用组件
    │   ├── SearchBar.vue       # 搜索框
    │   ├── TrackCard.vue       # 曲目卡片
    │   ├── TrackList.vue       # 曲目列表
    │   ├── ImportPanel.vue     # 导入面板（本期新增）
    │   ├── SheetRenderer.vue   # 五线谱渲染区
    │   ├── PianoKeyboard.vue   # 虚拟钢琴键盘
    │   ├── SplitPane.vue       # 可拖拽分隔条（布局调节）
    │   └── PlaybackBar.vue     # 播放控制条
    │
    ├── composables/            # 可复用的逻辑模块（Vue 3 组合式函数）
    │   ├── usePlayback.js      # 播放状态与时间轴驱动
    │   ├── useMidiParser.js    # MIDI 文件解析
    │   ├── useSheetRenderer.js # 乐谱渲染控制
    │   ├── useAudio.js         # 音频合成与播放
    │   └── useLocalImport.js   # 本地文件导入状态（本期新增）
    │
    └── utils/                  # 工具函数
        ├── search.js           # 曲目搜索过滤
        ├── timeFormat.js       # 时间格式化（如 01:23）
        ├── importFile.js       # 本地文件校验与读取（本期新增）
        └── noteMapping.js      # 音符与琴键的映射换算
```

### 2.2 各目录职责

| 目录 | 职责 | 说明 |
|---|---|---|
| `public/` | 原样复制到最终网站的文件 | 乐谱、MIDI、封面图放这里；构建时不做处理 |
| `src/data/` | 数据层 | 曲目元信息（曲名、作曲者等），**不含实际乐谱内容** |
| `src/views/` | 页面 | 对应 PRD 的两个页面：曲库页、播放页 |
| `src/components/` | 界面组件 | 一个组件管一块界面，便于单独修改 |
| `src/composables/` | 逻辑模块 | **核心逻辑集中在这里**（播放、解析、渲染、音频），与界面分离 |
| `src/utils/` | 工具函数 | 纯函数，输入输出确定，好测试 |

**为什么这样分**：
- **界面（views/components）与逻辑（composables）分开** —— 界面改动不影响播放逻辑，播放逻辑改动不影响界面
- **数据（data）与文件（public）分开** —— 元信息和实际文件各管各的

---

## 三、数据对象及字段

### 3.1 曲目对象（存储在 `src/data/tracks.json`）

```json
{
  "id": "track-01",
  "title": "致爱丽丝",
  "composer": "贝多芬",
  "grade": 3,
  "duration": 180,
  "sheetFile": "/sheets/track-01.musicxml",
  "midiFile": "/midi/track-01.mid",
  "description": "贝多芬钢琴小品，入门经典曲目",
  "coverImage": "/covers/track-01.jpg"
}
```

| 字段 | 类型 | 必填 | 说明 | 对应 PRD |
|---|---|---|---|---|
| `id` | 字符串 | ✅ | 曲目唯一标识，同时用于拼接文件名 | 6.1 |
| `title` | 字符串 | ✅ | 曲名 | 6.1 |
| `composer` | 字符串 | ✅ | 作曲者 | 6.1 |
| `grade` | 数字 | ✅ | **曲目级数**（1–10，参照钢琴考级）| 6.1 |
| `duration` | 数字 | ✅ | 曲目总时长（**单位：秒**）| 6.1 |
| `sheetFile` | 字符串 | ✅ | 乐谱文件路径（MusicXML 格式）| 6.1 |
| `midiFile` | 字符串 | ✅ | MIDI 文件路径 | 6.1 |
| `description` | 字符串 | ❌ | 曲目简介 | 6.1 |
| `coverImage` | 字符串 | ❌ | 封面图路径，缺省时卡片显示默认样式 | 6.1 |
| `isLocal` | 布尔 | ❌ | **本期新增**。`true` = 用户本地导入的曲目（仅在内存中存在，不写入 `tracks.json`）；内置曲目省略此字段或为 `false` | 6.1.1 |

**整个 `tracks.json` 的结构**：一个数组，每首曲目是一个对象。

```json
[ { "id": "track-01", ... }, { "id": "track-02", ... } ]
```

### 3.2 播放状态对象（运行时内存，不存文件）

```json
{
  "currentTrackId": "track-01",
  "isPlaying": false,
  "currentPosition": 0,
  "playbackSpeed": 1.0,
  "trackMode": "both",
  "metronomeOn": false,
  "isLoading": false,
  "errorMessage": null
}
```

| 字段 | 类型 | 默认值 | 取值 | 说明 | 对应 PRD |
|---|---|---|---|---|---|
| `currentTrackId` | 字符串 / null | `null` | 曲目 id | 当前曲目 | 6.2 |
| `isPlaying` | 布尔 | `false` | — | 是否正在播放 | 6.2 |
| `currentPosition` | 数字 | `0` | 0 到 duration | 当前播放位置（**单位：秒**）| 6.2 |
| `playbackSpeed` | 数字 | `1.0` | 0.5 – 1.5 | 播放速度倍率 | 6.2 |
| `trackMode` | 字符串 | `"both"` | `both` / `left` / `right` | 分轨模式 | 6.2 |
| `metronomeOn` | 布尔 | `false` | — | 节拍器开关 | 6.2 |
| `isLoading` | 布尔 | `false` | — | 是否正在加载曲目数据 | 6.3 |
| `errorMessage` | 字符串 / null | `null` | — | 错误提示文案，null 表示无错误 | 6.3 |

### 3.3 界面状态对象（运行时内存）

```json
{
  "searchKeyword": "",
  "layoutRatio": 0.55
}
```

| 字段 | 类型 | 默认值 | 说明 | 对应 PRD |
|---|---|---|---|---|
| `searchKeyword` | 字符串 | `""` | 曲库页搜索关键词 | 6.3 |
| `layoutRatio` | 数字 | `0.55` | 谱面区高度占可用高度的比例（0.2 – 0.8）| 6.2 |

### 3.4 数据来源说明（重要）

| 数据 | 从哪来 | 到哪去 |
|---|---|---|
| 曲目元信息（`tracks.json`）| 项目仓库 → 构建后随网站发布 | 浏览器加载后存在内存 |
| 乐谱文件（MusicXML）| `public/sheets/` 文件夹 | 播放页请求后交给渲染逻辑 |
| MIDI 文件 | `public/midi/` 文件夹 | 播放页请求后交给解析逻辑 |
| **用户本地选择的文件** | **用户自己的电脑** | **只读进浏览器内存，不上传**（见 5.5 流三）|

> **两条来源必须走同一套下游处理**：无论数据来自 `public/` 还是用户本机，解析后都产出**结构完全相同**的 `MidiData` 和乐谱对象。这样播放页的代码只需要处理一种情况，也保证了"导入的曲目与内置曲目表现一致"。区别只在于 `isLocal` 标记（PRD 6.1.1）。

---

## 四、接口列表

### 4.1 后端 API：本项目无

> **明确声明：本项目没有任何后端 API。**
>
> **理由**：项目采用纯静态架构，所有数据（曲目元信息、乐谱、MIDI）以文件形式打包在网站内，浏览器直接读取本项目的文件，**无需任何网络请求到自建服务器**。
>
> 因此不存在"请求地址""请求方法""返回格式"这类 API 定义。PRD 中定义的"数据字段"（第 6 节）描述的是**文件内容的结构**，不是接口返回的结构。

**唯一的网络请求**：浏览器下载本网站自己的静态文件（HTML、JS、CSS、乐谱、MIDI）。这不是"应用 API"，而是浏览器加载页面的常规行为，由托管平台（GitHub Pages）提供。

### 4.2 前端内部模块接口

虽然没有网络 API，但**代码内部模块之间**有明确的约定。这些约定让各模块能独立开发与替换。

#### 4.2.1 `useMidiParser` —— MIDI 解析模块

| 方法 | 输入 | 输出 | 说明 |
|---|---|---|---|
| `parseMidi(url)` | MIDI 文件路径（内置曲目用）| `Promise<MidiData>` | 读取并解析远程 MIDI 文件 |
| `parseMidiFromFile(file)` | `File` 对象（导入用）**本期新增** | `Promise<MidiData>` | 直接从用户选中的本地文件解析，**不经过网络** |
| `getEventsAt(time)` | 时间（秒）| `NoteEvent[]` | 取出某个时刻正在发声的音符 |

**`MidiData` 结构**：

```json
{
  "duration": 180,
  "tempo": 120,
  "tracks": [
    {
      "hand": "left",
      "notes": [
        { "midi": 60, "startTime": 0.0, "duration": 0.5, "velocity": 80 }
      ]
    }
  ]
}
```

| 字段 | 说明 |
|---|---|
| `duration` | 总时长（秒）|
| `tempo` | 速度（BPM），用于节拍器 |
| `tracks[].hand` | 该轨属于左手还是右手（`left` / `right`）|
| `notes[].midi` | MIDI 音高编号（0–127），用于对应琴键 |
| `notes[].startTime` | 起始时间（秒）|
| `notes[].duration` | 持续时长（秒）|
| `notes[].velocity` | 力度（0–127）|

#### 4.2.2 `useSheetRenderer` —— 乐谱渲染模块

| 方法 | 输入 | 输出 | 说明 |
|---|---|---|---|
| `loadSheet(url)` | 乐谱文件路径（内置曲目用）| `Promise<void>` | 加载并渲染乐谱到指定容器 |
| `loadSheetFromText(text)` | MusicXML 文本内容（导入用）**本期新增** | `Promise<void>` | 直接渲染内存中的乐谱文本，**不经过网络** |
| `highlightNotes(noteIds)` | 音符标识数组 | `void` | 高亮指定音符（**联动流一的核心**）|
| `scrollToNote(noteId)` | 音符标识 | `void` | 滚动到指定音符位置 |
| `clearHighlight()` | 无 | `void` | 清除所有高亮 |

#### 4.2.3 `useAudio` —— 音频模块

| 方法 | 输入 | 输出 | 说明 |
|---|---|---|---|
| `init()` | 无 | `Promise<void>` | 初始化音频环境（**需用户首次交互触发**，见 6.2）|
| `playNote(midi, duration, velocity)` | 音高、时长、力度 | `void` | 播放单个音符 |
| `stopAll()` | 无 | `void` | 停止所有正在发声的音符 |
| `setMetronome(on, tempo)` | 开关、速度 | `void` | 控制节拍器 |

#### 4.2.4 `usePlayback` —— 播放控制模块（核心）

| 方法 | 输入 | 输出 | 说明 |
|---|---|---|---|
| `loadTrack(trackId)` | 曲目 id | `Promise<void>` | 加载曲目（含乐谱与 MIDI）|
| `play()` | 无 | `void` | 开始播放 |
| `pause()` | 无 | `void` | 暂停播放 |
| `seek(position)` | 目标位置（秒）| `void` | 跳转到指定位置 |
| `setSpeed(speed)` | 倍率 | `void` | 设置播放速度 |
| `setTrackMode(mode)` | 分轨模式 | `void` | 切换分轨 |

**对外暴露的响应式状态**：`isPlaying`、`currentPosition`、`duration`、`playbackSpeed`、`trackMode`、`metronomeOn`、`isLoading`、`errorMessage`

#### 4.2.5 `utils/search.js` —— 搜索过滤

| 方法 | 输入 | 输出 | 说明 |
|---|---|---|---|
| `filterTracks(tracks, keyword)` | 曲目数组、关键词 | 过滤后的曲目数组 | 按**曲名或作曲者**模糊匹配，**不区分大小写** |

#### 4.2.6 `utils/noteMapping.js` —— 音符与琴键映射

| 方法 | 输入 | 输出 | 说明 |
|---|---|---|---|
| `midiToKeyIndex(midi)` | MIDI 编号（0–127）| 键盘位置索引（0–87）| 换算琴键在 88 键中的位置 |
| `isBlackKey(midi)` | MIDI 编号 | 布尔 | 判断是黑键还是白键 |
| `isLeftHand(midi)` | MIDI 编号 | 布尔 | 判断是否在左手常用音域（**仅作默认值，最终以 MIDI 分轨为准**）|

#### 4.2.7 `utils/importFile.js` —— 本地文件校验与解析（**本期新增**）

对应 PRD 3.4 与异常 E11–E15，是"用户本地文件流"的入口模块。

| 方法 | 输入 | 输出 | 说明 |
|---|---|---|---|
| `validateFile(file, expectedType)` | `File` 对象、期望类型（`midi` / `sheet`）| `{ ok: boolean, reason?: string }` | 检查扩展名、文件大小（≤10 MB）、文件头特征；不通过时返回**可直接展示给用户**的原因文案 |
| `readMidiFile(file)` | `File` 对象 | `Promise<MidiData>` | 读成二进制后交给 MIDI 解析模块，返回与内置曲目**完全相同**的结构 |
| `readSheetFile(file)` | `File` 对象 | `Promise<string>` | 读成文本（`.mxl` 需先解压）返回 MusicXML 字符串 |
| `checkCompatibility(midiData, sheetMeta)` | MIDI 数据、谱面元信息 | `{ matched: boolean, warning?: string }` | 比对时长与音符数，用于 E13 的不匹配警告 |

**设计要点**：

- 输出的 `MidiData` 与内置曲目走同一条解析路径 → **播放模块无需区分数据来源**，这是"导入的曲目表现与内置曲目一致"（PRD 验收 I6）的技术保证
- `validateFile` 把校验逻辑集中在一处，避免校验规则散落在界面组件里
- 所有函数**不产生网络请求**

#### 4.2.8 `useLocalImport` —— 导入状态管理（**本期新增**）

| 方法 | 输入 | 输出 | 说明 |
|---|---|---|---|
| `selectFile(type, file)` | 类型、`File` 对象 | `void` | 记录用户选中的文件并触发校验 |
| `importAndPlay()` | 无 | `Promise<void>` | 两个文件都就绪后执行解析，并跳转播放页 |
| `reset()` | 无 | `void` | 清空已选文件与错误提示（重新导入用）|

**对外暴露的响应式状态**：`midiFile`、`sheetFile`、`canImport`（两个文件是否都就绪）、`importError`（错误文案）

> `canImport` 直接驱动"导入"按钮的可用状态，对应 PRD 的 I4。


---

## 五、数据流

### 5.1 数据流图

```
┌──────────────────────── 流一 · 曲目数据流（加载时一次）────────────────────────┐
│                                                                              │
│   项目仓库文件          GitHub Pages         浏览器                          │
│  ┌────────────┐        ┌────────────┐     ┌──────────────────┐              │
│  │ tracks.json│  推送  │  静态托管   │ 下载 │  解析与渲染       │              │
│  │ sheets/*   │ ─────► │  (CDN)     │ ───►│                  │              │
│  │ midi/*     │        │            │     │  ┌────────────┐  │              │
│  └────────────┘        └────────────┘     │  │ 五线谱      │  │              │
│                                            │  └────────────┘  │              │
│                                            │  ┌────────────┐  │              │
│                                            │  │ 虚拟键盘    │  │              │
│                                            │  └────────────┘  │              │
│                                            └──────────────────┘              │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────── 流二 · 用户操作流（每次播放）──────────────────────────┐
│                                                                              │
│   用户操作           播放状态              三处同步响应                        │
│  ┌────────────┐      ┌────────────┐      ┌──────────────┐                   │
│  │ 播放/暂停   │      │currentPos  │      │ 谱面高亮     │                   │
│  │ 拖拽进度条  │ ───► │isPlaying   │ ───► │ 琴键点亮     │                   │
│  │ 调速/分轨   │ 写入 │speed/mode  │ 驱动 │ 声音发声     │                   │
│  └────────────┘      └────────────┘      └──────┬───────┘                   │
│                             ▲                    │                           │
│                             └────────────────────┘                           │
│                              位置持续推进（闭环）                              │
│                                                                              │
│  ┌────────────────────────────────────┐                                      │
│  │ 全程无服务器参与                    │                                      │
│  └────────────────────────────────────┘                                      │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────── 流三 · 用户本地文件流（本期新增）──────────────────────┐
│                                                                              │
│   用户电脑上的文件         浏览器内存              与流一相同的下游             │
│  ┌────────────┐ 选文件  ┌────────────┐  解析   ┌──────────────┐              │
│  │ xxx.mid    │ ─────► │  File 对象  │ ─────► │ 五线谱+键盘  │              │
│  │ xxx.musicxml│        │ (二进制数据) │        │ + 声音联动   │              │
│  └────────────┘        └────────────┘        └──────────────┘              │
│        │                                                                     │
│        └── 文件始终留在本机，不上传 ──┘                                       │
└──────────────────────────────────────────────────────────────────────────────┘
```

**三张图的关系**：流一提供"有哪些曲目"，流三提供"用户自己带的曲目"——**两条来源最终汇入同一个播放链路**（流二）。

### 5.2 流一：曲目数据流

**一句话**：**曲目数据打包在项目里，随网页一起下载到浏览器，由浏览器解析成乐谱和键盘。**

| 步骤 | 发生在哪 | 做什么 | 数据形态 |
|---|---|---|---|
| 1 | 开发者（你）的电脑 | 编写 `tracks.json`，放入乐谱与 MIDI 文件 | 本地文件 |
| 2 | Git 推送 | 文件上传到 GitHub 仓库 | 仓库文件 |
| 3 | GitHub Pages | 自动把仓库内容作为网站发布 | 托管文件 |
| 4 | 用户打开网页 | 浏览器下载 HTML/JS/CSS 及曲目数据 | 网络传输 |
| 5 | 浏览器解析 | 解析 `tracks.json` → 生成曲目列表；解析 MusicXML → 渲染五线谱；解析 MIDI → 得到音符时间轴 | 内存中的数据对象 |

**特点**：这条流**只在页面加载时跑一次**，之后数据不变（除非你改了文件并重新推送）。

### 5.3 流二：用户操作流

**一句话**：**用户的操作只改变浏览器里的播放状态，播放状态驱动谱面、键盘、声音三处同步响应。**

| 步骤 | 触发 | 做什么 |
|---|---|---|
| 1 | 用户点击"播放" | 把 `isPlaying` 改为 `true` |
| 2 | 时间轴推进 | 每帧更新 `currentPosition`（或按 MIDI 事件推进）|
| 3 | 查当前音符 | 用 `getEventsAt(currentPosition)` 取出此刻正在发声的音符 |
| 4 | 三处同时响应 | ① 谱面高亮这些音符 ② 键盘点亮对应琴键 ③ 播放对应声音 |
| 5 | 循环 | `currentPosition` 持续推进，回到第 3 步，直到曲目结束 |

**用户的其他操作分别改变什么**：

| 操作 | 改变的状态 | 立即结果 |
|---|---|---|
| 拖拽进度条 | `currentPosition` | 谱面滚动到新位置，键盘高亮同步更新 |
| 调速 | `playbackSpeed` | 时间轴推进速度变化，声音与联动节奏同步变化 |
| 切换分轨 | `trackMode` | 只播放所选声部的音符，另一声部静音；谱面与键盘仍显示全部（用颜色区分）|
| 开关节拍器 | `metronomeOn` | 开始/停止按曲目速度发声的节拍 |
| 拖拽分隔条 | `layoutRatio` | 谱面区与键盘区高度实时变化 |

### 5.4 一句话总结（Day 5 要掌握的内容）

> **曲目数据（MIDI + 乐谱文件）打包在项目里，随网页一起下载到浏览器；浏览器解析它，一路驱动五线谱渲染、虚拟键盘点亮和声音播放；用户的操作指令只改变浏览器里的播放状态，不发给任何服务器。**
>
> **（本期新增）** 用户也可以从自己的电脑选两个文件（MIDI + MusicXML）导入，走的是**同一条解析与播放链路**——区别只是数据来源从"网站下载"变成了"本地读取"，且本地文件永远不会上传。

### 5.5 流三：用户本地文件流（本期新增，对应 PRD 3.4）

> 与流一、流二并列的第三条数据流。**这条流的特点是：文件从头到尾不离开用户电脑。**

```
用户电脑上的文件
      │
      │ ① 用户点击"导入乐谱"，在系统文件选择框里选文件
      ▼
浏览器内存（File 对象）
      │
      │ ② 用 FileReader / File.arrayBuffer() 读成二进制
      ▼
内存中的原始数据
      │
      ├──► MIDI 二进制 ──► MIDI 解析模块 ──► 音符事件数组（时间轴）
      │
      └──► MusicXML 文本 ─► 乐谱渲染模块 ──► 五线谱图形
                                    │
                                    ▼
                        与流一完全相同：播放状态 → 三处同步响应
```

**与流一的关键差异**：

| 对比项 | 流一（内置曲库） | 流三（用户导入） |
|---|---|---|
| 数据来源 | 项目仓库里的 `public/sheets`、`public/midi` | 用户本机的任意位置 |
| 获取时刻 | 页面加载时批量拉取 | 用户点"导入"的那一刻 |
| 是否经过网络 | 是（从 GitHub Pages 下载） | **否，完全不经网络** |
| 是否入库 | 是（写在 `tracks.json` 里） | **否，只存在于当前会话内存** |
| 刷新页面后 | 仍在 | **消失**（需重新导入） |

**功能定义（写进 PRD 3.4 的内容）**：

用户在曲库页点击「导入乐谱」→ 打开导入面板 → **分别选择两个文件**：

| 文件 | 作用 | 支持的扩展名 |
|---|---|---|
| **MIDI 文件** | 提供**时间轴**：何时弹哪个音、多长、多响、左右手分轨 | `.mid` / `.midi` |
| **MusicXML 文件** | 提供**谱面排版**：五线谱怎么画、符头符杆、连音线、调号拍号 | `.xml` / `.musicxml` / `.mxl` |

两个文件都选好 → 点击「导入播放」→ 浏览器把它们读进内存并解析 → 进入播放页，谱面 + 键盘 + 声音三处联动。

> **为什么必须选两个文件？** 这是整个功能的核心难点。
>
> 让谱面和键盘同步跳动，需要**两份性质完全不同的数据**：
> - 谱面要知道"这个音符画在五线谱的哪根线、多长" —— 只有 MusicXML 有
> - 播放要知道"这个音符在第 3.2 秒响起、持续 1.5 秒" —— 只有 MIDI 有
>
> 用户手里通常只有其中一份：从网上下载的多半是 MIDI（能听不能看谱），从乐谱网站下载的多半是 PDF 或 MusicXML（能看不能听）。**一份文件无法同时满足两者**。

**三个可选方向（已比较）**：

| 方向 | 做法 | 优点 | 缺点 | 结论 |
|---|---|---|---|---|
| **A** | 只要 MIDI，不显示五线谱，改成"瀑布流"下落方块 | 最简单，只需一个文件 | **偏离项目定位**（本项目核心是五线谱+键盘联动，去掉五线谱等于砍掉一半） | 不采用 |
| **B** | 要求用户**同时提供** MIDI + MusicXML 两个文件 | 最可靠，两份数据各司其职，联动精度最高 | 用户要自己凑齐两个文件 | ✅ **本期采用** |
| **C** | 只要 MusicXML，从乐谱**推算**时间轴 | 用户只需一个文件，体验最省事 | 从乐谱推算时间轴要处理速度变化、连音、装饰音、反复记号，**技术风险高**，精度无法保证 | 列入后续功能（PRD 四） |

**降级与容错**（对应 PRD 的 E11–E15）：

| 情况 | 技术处理 |
|---|---|
| 扩展名不在白名单 | 前端读文件头校验（不只看扩展名），不通过则不进入下一步，面板内提示 |
| 只选了一个文件 | "导入"按钮 `disabled`，提示还缺哪一个 |
| 文件损坏 / 解析抛异常 | `try/catch` 包住解析逻辑，捕获后回到导入面板并提示，用户可重选 |
| 文件超过 10 MB | 读取前检查 `file.size`，超限直接拒绝，不读进内存 |
| 两份数据不匹配 | 解析出各自时长后比对，差异超阈值时在播放页顶部显示警告，**但不阻断播放**（同 E6） |

**明确不做（本期）**：

- 不上传、不保存、不分享导入的曲目（这些都需要后端，见 PRD 四"用户上传曲谱供他人使用"）
- 不支持 PDF（PDF 是图像，没有音符数据，无法解析）
- 不支持只给 MusicXML 的单文件导入（方向 C，见上表）

---

## 六、错误处理

> 对应 PRD 第七节的异常场景（E1–E15），说明**在技术层面如何处理**。

### 6.1 错误处理总原则

| 原则 | 说明 |
|---|---|
| **降级优先** | 一个部分失效时，尽量让其他部分仍可用（如谱面渲染失败，但播放仍能进行）|
| **不白屏** | 任何错误都要有可见提示，不允许出现空白页面 |
| **不影响核心链路** | 非核心功能出错（如封面图缺失）不阻断播放 |
| **记录可排查** | 出错时在浏览器控制台输出具体原因，便于定位 |

### 6.2 各异常场景的技术处理

| 编号 | 异常场景（PRD）| 技术层面的处理 |
|---|---|---|
| E1 | 搜索无结果 | `filterTracks` 返回空数组 → 界面显示"未找到相关曲目"提示，并提供清空按钮 |
| E2 | 曲目数据加载失败 | `usePlayback.loadTrack` 的 Promise 被拒绝 → 设置 `errorMessage`，界面显示错误提示与"返回曲库"按钮 |
| E3 | 曲目文件缺失 | 文件请求返回失败（404 等）→ 与 E2 同样处理，并在控制台输出缺失**具体文件名** |
| E4 | 谱面解析失败 | 渲染模块单独捕获异常 → 谱面区显示"谱面无法显示"，**音频与键盘模块继续正常工作**（降级）|
| E5 | 音频无法播放 | ① 检测音频能力，不支持时提示<br>② 浏览器自动播放策略：**必须在用户首次点击后才初始化音频**（技术关键点）<br>③ 初始化失败时提示"点击以启用声音" |
| E6 | 两份数据不匹配 | 加载完成后比对乐谱与 MIDI 的时长差 → 差异超阈值时设置警告状态，界面顶部提示，**不阻断播放** |
| E7 | 进度拖到末尾 | `currentPosition` 达到 `duration` → 自动 `pause()`，`isPlaying` 置为 `false` |
| E8 | 键盘滚动到边界 | 滚动容器到达边界后停止，容器宽度按 88 键总宽固定，不出现空白 |
| E9 | 曲库为空 | `tracks.json` 为空数组 → 界面显示"曲库暂无内容"，不渲染列表容器 |
| E10 | 拖拽分隔条到极端 | `layoutRatio` 做范围约束（0.2 – 0.8），超出时钳制到边界值 |
| E11 | 导入文件格式不支持 | 读取文件头的 magic number 校验真实格式（不只看扩展名）→ 不通过则回退到导入面板并提示，**不创建播放状态** |
| E12 | 只选了一个文件 | 导入按钮绑定 `:disabled="!midiFile \|\| !sheetFile"`，并用条件文案提示缺哪一个 |
| E13 | 导入的谱面与 MIDI 不匹配 | 同 E6：比对两份数据的时长（和音符数）→ 超阈值时置警告状态，播放页顶部提示，**不阻断播放**；任一份解析失败时按 E4 降级 |
| E14 | 导入文件解析失败 | 解析函数整体包 `try/catch`，捕获后保持导入面板打开、显示提示文案，用户可重新选文件（不跳页、不清空已选中的另一个文件） |
| E15 | 导入文件过大 | 读取前判断 `file.size > 10 * 1024 * 1024`，超限直接拒绝并把文件名回显在提示里，不读进内存 |

### 6.3 三个技术关键点

1. **音频自动播放限制（E5）**
   现代浏览器**禁止页面在没有用户交互时自动发声**。因此音频环境**必须在用户第一次点击后才初始化**。这是本项目必须处理的技术约束，不是可选的优化。

2. **两份数据的时长对齐（E6）**
   乐谱（MusicXML）和 MIDI 是两个独立文件，**"同一首曲子的同一版本"需要在准备数据时人工保证**。若两者描述的音符不一致，会出现"谱面高亮的位置和听到的声音对不上"。
   **处理策略**：本期采用"人工核验 + 运行时警告"，不做自动对齐（自动对齐属于复杂技术，不在 28 天范围内）。

3. **88 键键盘的宽度问题**
   88 键在普通屏幕上无法完整显示。**处理方式**：键盘区宽度按 88 键总宽度固定，通过横向滚动查看。这与 PRD 的 `B4`、`B5` 验收标准一致。

---

## 七、环境变量

### 7.1 本项目需要哪些环境变量

> **结论：本期不需要任何环境变量。**

| 常见用途 | 本项目是否需要 | 原因 |
|---|---|---|
| 数据库连接串 | ❌ 不需要 | 无数据库 |
| API 密钥 | ❌ 不需要 | 无后端、无第三方 API 调用 |
| 服务器地址 | ❌ 不需要 | 无后端 |
| 部署密钥 | ⚠️ 不需要写入文件 | 部署通过 GitHub 的推送机制完成，凭据由系统管理（Windows 凭据管理器），**不写入项目文件** |
| 运行模式（开发/生产）| ⚠️ 由构建工具自动处理 | Vite 内置机制，无需手动配置 |

### 7.2 为什么仍保留 `.env` 忽略规则

项目的 `.gitignore` 中已包含 `.env` 及各类密钥文件的忽略规则（Day 2 建立）。**本期不用，但保留**，原因：

1. **为后续迭代预留** —— 若将来加入后端或第三方服务，会需要密钥管理，规则已就位
2. **防止误提交** —— 若将来无意中创建了 `.env` 文件，忽略规则会自动挡住
3. **零成本** —— 保留不需要任何代价

### 7.3 一条必须遵守的规则

> **任何密钥、令牌、密码，永远不写入代码文件，不提交到仓库。**
>
> 这是 `AGENTS.md` 第五条第 3 款的约定，也是本项目当前的实践（推送凭据由系统凭据管理器保管，不在项目文件内）。

---

## 八、部署与迁移注意事项

### 8.1 部署方式

| 项 | 内容 |
|---|---|
| **平台** | GitHub Pages |
| **来源** | 本项目的 GitHub 仓库 |
| **触发方式** | 推送代码到指定分支后，GitHub 自动发布 |
| **访问地址** | `https://shy1234567-777.github.io/notekey/`（**以实际配置为准**）|
| **费用** | 免费 |

### 8.2 部署前提（需注意的配置项）

| 注意点 | 说明 |
|---|---|
| **构建产物** | Vue 项目需要**先构建**（把源码编译成浏览器可读的文件），再把产物发布。GitHub Pages 需要配置为发布**构建产物目录**，而非源码目录 |
| **资源路径** | 网站部署在子路径下（`/notekey/`），**资源引用必须使用相对路径或配置正确的基准路径**，否则会出现"图片/文件 404" |
| **HTTPS** | GitHub Pages 默认提供，无需配置 |
| **音频文件大小** | MIDI 文件很小（几十 KB），MusicXML 也不大，**不构成部署障碍** |

### 8.3 部署步骤（概念流程，Day 7+ 实际执行）

1. 本地开发完成 → 执行构建 → 得到构建产物目录
2. 提交并推送到 GitHub
3. 在 GitHub 仓库设置中指定发布来源
4. 等待 1–2 分钟，访问网址验证

### 8.4 迁移注意事项（未来可能的变化）

| 未来可能的变化 | 影响范围 | 迁移建议 |
|---|---|---|
| **加入后端**（账号、上传共享）| 需要新增服务端；前端由"直接读文件"改为"请求接口" | 前端已用 `composables` 隔离逻辑，把"读文件"换成"请求接口"即可，界面层基本不动 |
| **数据源改为数据库** | `tracks.json` 被接口取代 | 只需修改数据加载位置，`TrackCard` 等界面组件不变 |
| **增加曲目数量** | 仅需改 `tracks.json` + 放入文件 | 无需改代码 |
| **迁移到其他托管平台** | 部署配置 | 纯静态站点可迁移到任意静态托管平台，代码无需修改 |
| **改为桌面应用** | 架构变化 | 需重新评估，但 Vue 代码可复用 |

### 8.5 数据备份与版本管理

| 项 | 做法 |
|---|---|
| 代码与文档 | 由 Git 管理，每次提交即备份 |
| 曲目数据（JSON/乐谱/MIDI）| 与代码一同提交，同样受版本管理 |
| 用户数据 | **无（本项目不收集任何用户数据）** |

---

## 九、待确认事项

| 编号 | 待确认内容 | 影响 | 何时解决 |
|---|---|---|---|
| T1 | 具体的 10 首曲目清单（PRD 的 Q2）| 影响数据准备 | Day 6 |
| T2 | **乐谱与 MIDI 从哪获取**（需为公版曲目，且两份文件描述同一版本）—— **本期新增"导入功能"后，这一项的重要性进一步上升**：内置 10 首必须两份齐全，用户导入也要求用户自己凑齐两份 | 影响核心功能能否成立 | Day 6 前必须确认 |
| T3 | 分别用哪个库处理乐谱渲染和 MIDI 解析 | 影响开发复杂度 | 开始写代码前确定（Day 7 前）|
| T4 | 左手/右手如何区分（依赖 MIDI 文件内部的轨道划分）| 影响分轨功能的实现方式 | Day 6–7 |
| T5 | 曲目的实际时长（写进 `tracks.json` 的 `duration`）| 影响进度条与时间显示 | 数据准备时 |
| T6 | GitHub Pages 的发布分支与路径配置 | 影响部署能否成功 | Day 7+ 部署时 |
| T7 | **导入面板的展示形式**（弹窗 / 从侧边展开 / 独立页面）| 影响界面实现复杂度 | 写曲库页前确定（Day 7 前）|

### 9.1 T2 的具体执行方案（三步）

因为 T2 直接决定**内置曲库能不能做出来**，这里把执行动作写细：

| 步骤 | 做什么 | 具体做法 | 验收标准 |
|---|---|---|---|
| **① 找 MIDI** | 为 10 首曲目各找一份 MIDI | 从公开 MIDI 资源站下载（如 MuseScore 公开谱库、各类古典曲目 MIDI 合集）。**优先选带清晰左右手分轨的版本**（关系到 T4 的分轨功能）| 10 个 `.mid` 文件，每首都能在本机播放器里正常播放 |
| **② 找对应的 MusicXML** | 为同一首曲子找一份 MusicXML | **优先路径**：先找现成的同曲 MusicXML。<br>**兜底路径**：若找不到，用 MuseScore（免费桌面软件）打开 ① 的 MIDI，再"另存为 / 导出"成 MusicXML | 10 个 MusicXML 文件，能正常打开且与 MIDI 是同一首曲子 |
| **③ 核对"同一版本"** | 确认两份文件描述的是**同一个版本** | 逐首打开比对：**旋律走向一致、小节数接近、总时长接近**（允许小幅出入，但不能出现"结构不同"）。有差异时记录在案 | 每首曲目记一行结论：`一致` / `有差异（附说明）`|

> **第 ③ 步为什么必须做**：同名曲目在网络上有大量不同编配版本（如《致爱丽丝》有简化版、原版、各种改编版）。如果 MIDI 用的是简化版、MusicXML 用的是原版，谱面和声音就会对不上——这正是 PRD 异常场景 E6 / E13 要防的情况，也是本项目**最容易翻车的地方**。


---

## 十、附：技术栈名词速查

> 供后续查阅，每个术语一句话解释。

| 名词 | 一句话解释 |
|---|---|
| **前端** | 用户在浏览器里看到和操作的部分 |
| **后端** | 服务器上运行的、用户看不见的程序（本项目没有）|
| **数据库** | 集中存放数据、可被程序查询的地方（本项目没有）|
| **静态网站** | 内容预先做好，用户访问时直接下载，不需要服务器实时生成 |
| **框架** | 一套现成的代码结构，帮你组织项目，不用从零搭架子 |
| **Vue** | 一个前端框架，特点是"数据变了界面自动更新" |
| **响应式** | 数据变化时，用到这个数据的界面**自动**跟着变，不用手动刷新 |
| **组件** | 一块可复用的界面单元（如"一个曲目卡片"就是一个组件）|
| **构建** | 把源码编译、打包成浏览器能直接读的文件的过程 |
| **Vite** | 一个构建工具，Vue 官方推荐，负责开发时预览和发布前打包 |
| **部署** | 把做好的网站放到服务器上，让别人能通过网址访问 |
| **GitHub Pages** | GitHub 提供的免费静态网站托管服务 |
| **响应式状态** | 项目里"会变化的数据"，界面自动跟随它更新 |
| **时间轴推进** | 播放时按时间不断更新"当前播放到哪一秒"的过程 |
| **降级处理** | 某部分出错时，让它失效但保证其他部分仍能用 |
| **MIDI** | 一种描述"什么时候弹哪个音、弹多长、多响"的文件格式（不含声音本身）|
| **MusicXML** | 一种描述乐谱（音符在谱面上怎么排）的文件格式 |
| **File 对象** | 浏览器里代表"用户选中的那个本地文件"的东西，只有文件名、大小、内容，**不含路径**，也无法被网页自动读取 |
| **文件头校验** | 检查文件开头的几个字节（magic number）来判断真实格式，比只看扩展名可靠（防止把 `.txt` 改名成 `.mid`）|
| **白名单** | 只允许清单里列出的格式通过，其余一律拒绝（比"黑名单"更安全）|

---

*文档结束。下一步：Day 6 数据准备 / Day 7 开始开发。*
